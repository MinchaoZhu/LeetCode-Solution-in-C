#include <stdio.h>



/**
 * Return an array of size *returnSize.
 * Note: The returned array must be malloced, assume caller calls free().
 */
char** generateParenthesis(int n, int* returnSize) {
    int ln = 3, rn = 3;
    *returnSize = 0;
}






int main(void){
    int returnSize;
    char ** p = generateParenthesis(3,&returnSize);


    return 0;
}